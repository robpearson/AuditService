apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audit-service-staging
  namespace: argocd
  annotations:
    argo.octopus.com/project: "audit-service"
    argo.octopus.com/environment: "staging"
    argo.octopus.com/image-replace-paths: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
spec:
  project: default
  source:
    repoURL: https://github.com/robpearson/AuditService
    targetRevision: HEAD
    path: audit-service
    helm:
      releaseName: audit-service
      valueFiles:
        - values.yaml
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: audit-service-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
